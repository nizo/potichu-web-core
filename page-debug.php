<?php
/*
	$pages = get_pages();
	foreach ($pages as $page) {
		update_post_meta($page->ID, 'header', 'yes');
		update_post_meta($page->ID, 'header_title_bar', 'breadcrumbs_only');
		//echo get_post_meta($page->ID, 'header_title_bar', true) . ' ----';
	}
	*/
?>